-- Aggregate Functions

	-- Added to the column list in a query's SELECT Clause
	-- FunctionName (TableColumn)
	-- Add a descriptive Alise with AS

-- Common Aggregate Functions

	-- COUNT(*)
	-- COUNT (DISTINCY COlumn)
	-- COUNT_BIG (Column) - Counting rows exceeding billion items
	-- SUM (Column)
	-- AVG (Column) - Ignore Null Values
	-- MIN (COLUMN)
	-- MAX (Column)
	-- STDEV (Column)
	-- VAR (Column)
	-- VARP (Column)

-- Approximate Aggregate Functions
	--Used in big data applications where speed is more important then abosolute accuracy
	-- APPROX_COUNT_DISTINCT
	-- APPROX_PERCENTILE_CONT
	-- APPROX_PERCENTILE_DISC

